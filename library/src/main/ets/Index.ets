// ArkTS Lodash — Barrel Export (v4.18.1 port)
// Auto-generated by Phase 2 BUILD. Re-exports all public APIs.

// Types
export { type ValueCallback, type Comparator, type Path, type PropertyName, type Many, type PlainObject, type NotVoid } from './types'

// Internal (not re-exported — used only by other modules)

// Util — Stubs (no deps, available early)
export { stubArray, stubFalse, stubObject, stubString, stubTrue } from './util_stubs'

// Lang — Type Checks
export { isArguments, isArray, isArrayBuffer, isArrayLike, isArrayLikeObject, isBoolean, isDate, isElement, isEmpty, isError, isFinite, isFunction, isInteger, isLength, isMap, isNaN, isNil, isNull, isNumber, isObject, isObjectLike, isPlainObject, isRegExp, isSafeInteger, isSet, isString, isSymbol, isTypedArray, isUndefined, isWeakMap, isWeakSet } from './lang_typecheck'

// Lang — Comparison
export { eq, gt, gte, lt, lte, isEqual, isEqualWith, isMatch, isMatchWith, conformsTo } from './lang_compare'

// Lang — Conversion & Clone
export { castArray, toArray, toFinite, toInteger, toLength, toNumber, toPlainObject, toSafeInteger, toString, toPairs, toPairsIn, clone, cloneDeep, cloneDeepWith, cloneWith } from './lang_convert'

// Math
export { add, subtract, multiply, divide, ceil, floor, round, sum, sumBy, mean, meanBy } from './math_basic'
export { max, maxBy, min, minBy, clamp, inRange, random } from './math_extremum'

// String
export { camelCase, capitalize, kebabCase, lowerCase, lowerFirst, snakeCase, startCase, upperCase, upperFirst, deburr } from './string_case'
export { endsWith, startsWith, pad, padEnd, padStart, repeat, replace, split, trim, trimEnd, trimStart, truncate, words, parseInt, toLower, toUpper } from './string_manip'
export { escape, escapeRegExp, unescape } from './string_escape'

// Array
export { chunk, compact, concat, drop, dropRight, dropRightWhile, dropWhile, fill, flatten, flattenDeep, flattenDepth, fromPairs, head, initial, join, last, nth, reverse, slice, tail, take, takeRight, takeRightWhile, takeWhile } from './array_basic'
export { difference, differenceBy, differenceWith, intersection, intersectionBy, intersectionWith, union, unionBy, unionWith, uniq, uniqBy, uniqWith, without, xor, xorBy, xorWith, zip, zipObject, zipObjectDeep, zipWith, unzip, unzipWith } from './array_set'
export { findIndex, findLastIndex, indexOf, lastIndexOf, pull, pullAll, pullAllBy, pullAllWith, pullAt, remove, sortedIndex, sortedIndexBy, sortedIndexOf, sortedLastIndex, sortedLastIndexBy, sortedLastIndexOf, sortedUniq, sortedUniqBy } from './array_search'

// Collection
export { forEach, forEachRight, map, filter, reject, every, some, reduce, reduceRight, find, findLast, includes, size } from './collection_core'
export { countBy, groupBy, keyBy, partition, sortBy, orderBy, flatMap, flatMapDeep, flatMapDepth, invokeMap, sample, sampleSize, shuffle } from './collection_group'

// Function
export { after, before, ary, unary, negate, once, memoize, wrap, spread, flip } from './function_wrap'
export { bind, bindKey, bindAll, curry, curryRight, partial, partialRight, rearg, overArgs } from './function_bind'
export { debounce, throttle, defer, delay, cancelDebounce, flushDebounce, cancelThrottle, flushThrottle } from './function_timing'
export { flow, flowRight, over, overEvery, overSome, cond, conforms, matches, matchesProperty, iteratee } from './function_compose'

// Object
export { get, set, setWith, has, hasIn, unset, update, updateWith, at, result, invoke } from './object_access'
export { assign, assignIn, assignWith, assignInWith, merge, mergeWith, defaults, defaultsDeep, create, omit, omitBy, pick, pickBy, mapKeys, mapValues, invert, invertBy, transform } from './object_transform'
export { keys, keysIn, values, valuesIn, forOwn, forOwnRight, forIn, forInRight, findKey, findLastKey, functions, functionsIn } from './object_iterate'

// Util
export { attempt, constant, defaultTo, identity, noop, mixin, method, methodOf, nthArg, property, propertyOf, range, rangeRight, times, uniqueId, toPath } from './util_misc'

// Common aliases (lodash v4 convention)
export { forEach as each } from './collection_core'
export { head as first } from './array_basic'
export { assign as extend } from './object_transform'
export { tail as rest } from './array_basic'
export { toPairs as entries } from './lang_convert'
